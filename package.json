{
  "name": "taskflow-build",
  "version": "1.0.0",
  "private": true,
  "description": "Build & obfuscation toolchain for TaskFlow frontend",
  "scripts": {
    "build": "node build.js",
    "clean": "node build.js --clean"
  },
  "devDependencies": {
    "clean-css": "^5.3.3",
    "html-minifier-terser": "^7.2.0",
    "javascript-obfuscator": "^4.1.1"
  }
}
